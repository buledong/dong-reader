/**
 * Created by  blue dong on 2017/12/6.
 */
'use strict';
export const bookId = state => state.bookId;

export const chapterId = state => state.chapterId;

export const prevId = state => state.prevId;

export const nextId = state => state.nextId;

export const rankQuery = state => state.rankQuery;

export const searchQuery = state => state.searchQuery;

export const categoryQuery = state => state.categoryQuery;
